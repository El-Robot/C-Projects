    // if (nextValue == currentValue)
    // {
    //   if ((currentValue == 5 || currentValue == 50 || currentValue == 500) && currentValue == nextValue)
    //   {
    //     printf("[Error: %s is not a Roman numeral]", str);
    //     return 0;
    //   }
    //   consecutiveCount++;
    //   if (consecutiveCount == 3)
    //   {
    //     printf("[Error: %s is not a Roman numeral]", str);
    //     return 0;
    //   }
    // }
    // else
    // {
    //   consecutiveCount = 0;
    // }

    // if (nextValue > currentValue)
    // {
    //   if (currentValue == 5 || currentValue == 50 || currentValue == 500)
    //   {
    //     printf("[Error:Roman numeral %s does not exist]", str);
    //     return 0;
    //   }

    //   currentValue *= -1;
    // }
